import { Paper, Grid } from '@mui/material';

export const TourCard = () => {
    return (
        <Grid item xs={3}>
            <Paper elevation={3} square>
                    Hello
            </Paper>
        </Grid>

    );
};